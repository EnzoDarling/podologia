{% extends 'base.html.twig' %}
    {% block body %}
        <a href="/pacientes" class="btn btn-default">< Volver</a>
        <h2 class="page-header">Nuevo Paciente</h2>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}        
    {% endblock %}