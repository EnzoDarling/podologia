{% extends 'base.html.twig' %}
    {% block body %}
        <a href="/vademecum" class="btn btn-default">< Volver</a>
        <h2 class="page-header">Crear Posología</h2>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
    {% endblock %}